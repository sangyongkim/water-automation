# ==================================================
# HEC-RAS ↔ AutoCAD 연동 설정
# ★ 이 파일을 실제 프로젝트 경로에 맞게 수정하세요 ★
# ==================================================

# --------------------------------------------------
# [1] HEC-RAS 파일 경로
# --------------------------------------------------
# .prj  : HEC-RAS 프로젝트 파일 (예: "홍수분석.prj")
# .g0x  : Geometry 파일 — 단면 좌표가 담긴 파일
# .p0x.hdf : 계산 결과 HDF5 파일 — Step 3 결과가 저장됨

HECRAS_PROJECT_FILE  = r"C:\프로젝트\hec-ras\프로젝트명.prj"
HECRAS_GEOMETRY_FILE = r"C:\프로젝트\hec-ras\프로젝트명.g01"
HECRAS_HDF_FILE      = r"C:\프로젝트\hec-ras\프로젝트명.p01.hdf"

# --------------------------------------------------
# [2] AutoCAD DXF 파일 경로
# --------------------------------------------------
# DXF_FILE   : Step 1이 생성하고 AutoCAD가 열어 편집하는 도면
# PANEL_META : Step 1이 함께 생성하는 패널 위치 메타데이터 (자동 생성, 수정 불필요)

DXF_FILE   = r"C:\프로젝트\dwg\crosssections.dxf"
PANEL_META = r"C:\프로젝트\dwg\crosssections_panels.json"

# --------------------------------------------------
# [3] 중간 결과 파일 (Step 3 → Step 4)
# --------------------------------------------------
WSE_JSON_FILE = r"C:\프로젝트\dwg\wse_results.json"

# --------------------------------------------------
# [4] HEC-RAS 실행 파일 경로
# --------------------------------------------------
# Step 3에서 COM 연결 실패 시 대안 모드로 HEC-RAS를 직접 실행합니다.
# 설치된 버전에 맞게 경로를 확인하세요.
#
# 5.0.6 : C:\Program Files (x86)\HEC\HEC-RAS\5.0.6\Ras.exe
# 6.6   : C:\Program Files (x86)\HEC\HEC-RAS\6.6\Ras.exe

HECRAS_EXE = r"C:\Program Files (x86)\HEC\HEC-RAS\5.0.6\Ras.exe"

# --------------------------------------------------
# [5] HEC-RAS COM 클래스 ID
# --------------------------------------------------
# 설치된 HEC-RAS 버전에 따라 다릅니다.
# 정확한 ID는 check_env.py 를 실행하면 확인할 수 있습니다.
#
# 5.0.6 : "RAS506.HECRASController"
# 5.0.7 : "RAS507.HECRASController"
# 6.6   : "RAS66.HECRASController"

HECRAS_COM_ID = "RAS506.HECRASController"

# --------------------------------------------------
# [6] AutoCAD 레이어 이름 (AutoCAD 레이어명과 반드시 일치해야 합니다)
# --------------------------------------------------
LAYER_EXISTING = "현황단면"   # HEC-RAS 지형 — Step 1이 자동 생성
LAYER_PLANNED  = "계획단면"   # 사용자가 AutoCAD에서 직접 그리는 수정 단면
LAYER_FLOOD    = "홍수위"     # 홍수위선 — Step 1, Step 4가 자동 생성
LAYER_LEVEE    = "제방"       # 제방 위치 마커 — Step 1이 생성, 사용자가 이동 가능
LAYER_TEXT     = "단면표시"   # 텍스트 주석

# --------------------------------------------------
# [7] 레이어 색상 (AutoCAD 색상 번호)
# --------------------------------------------------
COLOR_EXISTING = 7   # 흰색
COLOR_PLANNED  = 1   # 빨강
COLOR_FLOOD    = 5   # 파랑
COLOR_LEVEE    = 3   # 초록
COLOR_TEXT     = 2   # 노랑

# --------------------------------------------------
# [8] 도면 레이아웃
# --------------------------------------------------
PANEL_SPACING = 50.0   # 횡단면 패널 사이 간격 (도면 단위)
TEXT_HEIGHT   = 1.0    # 텍스트 높이

# --------------------------------------------------
# [9] 홍수위 프로파일 지정
# --------------------------------------------------
# HEC-RAS 계획(plan)에 여러 유량 프로파일이 있을 경우 어느 것을 사용할지 지정합니다.
#
# ① 이름으로 지정 (권장): FLOOD_PROFILE_NAME 에 프로파일 이름을 입력
#      예) "100년빈도"  "200년빈도"  "PF 1"
#   Step 3 실행 시 HEC-RAS 에 등록된 전체 프로파일 목록을 출력하고
#   일치하는 이름을 자동 선택합니다.
#
# ② 번호로 지정: FLOOD_PROFILE_NAME 을 빈 문자열("")로 두고
#   FLOOD_PROFILE_INDEX 에 번호(1부터 시작)를 입력

FLOOD_PROFILE_NAME  = ""   # 프로파일 이름 (빈 문자열이면 아래 INDEX 사용)
FLOOD_PROFILE_INDEX = 1    # 이름 미지정 시 프로파일 번호 (1부터 시작)

# --------------------------------------------------
# [10] XDATA 앱 이름 (변경 불필요)
# --------------------------------------------------
XDATA_APP = "HECRAS_XS"
